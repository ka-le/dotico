machine:
  node:
    version: 6.2.2
deployment:
  staging:
    branch: master
    heroku:
      appname: doticos-v2
dependencies:
  post:
    - npm install -g bower
    - bower install
    - npm rebuild node-sass
test:
  override:
    - echo "test"
